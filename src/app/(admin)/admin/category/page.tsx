// Page catégorie admin (ancienne structure)
export default function AdminCategory() {
  return (
    <div style={{padding: 40}}>
      <h1 style={{fontSize: 32, fontWeight: 'bold'}}>Admin Category (ancienne structure)</h1>
      <p>Cette page est restaurée dans (admin)/admin/category/page.tsx</p>
    </div>
  );
}
