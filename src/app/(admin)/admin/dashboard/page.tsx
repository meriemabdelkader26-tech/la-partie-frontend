// Page dashboard admin (ancienne structure)
export default function AdminDashboard() {
  return (
    <div style={{padding: 40}}>
      <h1 style={{fontSize: 32, fontWeight: 'bold'}}>Admin Dashboard (ancienne structure)</h1>
      <p>Cette page est restaurée dans (admin)/admin/dashboard/page.tsx</p>
    </div>
  );
}
